apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: oac-model-cache-pvc
  namespace: maas
spec:
  storageClassName: openebs-local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi